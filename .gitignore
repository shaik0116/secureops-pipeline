# Terraform state — contains real Azure resource IDs and sensitive outputs
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
terraform.tfvars

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# Environment files — never commit real credentials
.env
.env.*

# Scan results — generated by pipeline, not source code
bandit-results.json
secrets-results.json
checkov-results.json
trivy-results.json
sca-results.json

# Personal study notes — not part of the project
learning.md
terraform.md

# OS files
.DS_Store
Thumbs.db
